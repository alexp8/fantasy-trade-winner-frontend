## Setup

### Tools
1. Install node.js
    * https://nodejs.org/en
2. Install Angular
    * `npm install -g @angular/cli`
    * `ng add @angular/material`
3. Run server
    * `fantasy-trade-winner-app/` `ng serve`
